import type {Metadata} from "next";import Link from "next/link";
export const metadata:Metadata={title:"Admin Access",robots:{index:false,follow:false}};
export default function Admin(){return <main className="auth-page"><section className="auth-shell glass" style={{maxWidth:800}}><div className="auth-story"><Link href="/" className="wordmark">BYLLD <b>X</b></Link><div><h1>PRIVATE<br/><em>ADMIN.</em></h1><p>Moderation, account enforcement, payments and reporting are isolated from the member product.</p></div><small>Server authorization required for every action</small></div><div className="auth-form"><span className="eyebrow">Restricted operations</span><h2>Admin sign in</h2><p>This route does not expose sample member data or administrative actions. Production access requires a verified admin claim checked on the server and in database policies.</p><label className="field"><span>Admin email</span><input type="email" autoComplete="username" placeholder="admin@bylldx.in"/></label><label className="field"><span>Password</span><input type="password" autoComplete="current-password" placeholder="••••••••••••"/></label><button className="button" disabled style={{opacity:.55,cursor:"not-allowed"}}>Connect admin identity provider</button><div className="notice" style={{marginTop:18,fontSize:10,lineHeight:1.6}}>Locked in this public preview. There is no client-side bypass, default password or public admin dataset.</div><div className="auth-links"><Link href="/">Back to public site</Link><Link href="/safety">Review safety rules</Link></div></div></section></main>}
